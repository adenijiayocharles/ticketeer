{
  "name": "ticketeer",
  "version": "1.0.0",
  "description": "ticket sales web api",
  "main": "app.js",
  "scripts": {
    "start-development": "export NODE_ENV=development && nodemon app.js",
    "start-production": "export NODE_ENV=production && nodemon app.js",
    "migrate": "node -e \"require('./migration').migrate()\"",
    "migrate:rollback": "node -e \"require('./migration').migrateRollback()\"",
    "migrate:reset": "node -e \"require('./migration').migrateReset()\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "http-status": "^1.6.2",
    "mysql2": "^3.6.0",
    "sequelize": "^6.32.1"
  },
  "devDependencies": {
    "prettier": "3.0.1",
    "sequelize-cli": "^6.6.1"
  }
}
